# hetic-bot
A simple Discord bot that automatically sends reminders 10 minutes before each class based on an iCal schedule (HETIC). It fetches the latest events from a calendar URL, displays upcoming courses with embeds, and lets users check the next class with a !prochain_cours command.
